overlapping intervals

my soln:nothing used ;-)

def overlappedInterval(parr,n):
    #code here
    parr.sort()
    # print(parr)
    i=0
    # for i in range(n-1):
    while(i<n-1):
        # print(i,parr[i][1],parr[i+1][0],parr[i][1],parr[i+1][1])
        if(parr[i][1]>=parr[i+1][0]):
            if(parr[i][1]<parr[i+1][1]):
                parr[i][1]=parr[i+1][1]
            # else:
            parr.pop(i+1)
            n-=1
        else:
            i+=1
    return parr

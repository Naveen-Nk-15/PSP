my soln:

test case not given properly:workks correctly

hashmap can be used for O(n)
store a in hashmap
traverse b =>
check b-x in hashmap then print b and key in hashmap


#include <stdio.h>

int main() {
	//code
	int nt;
	scanf("%d",&nt);
	for(int i=0;i<nt;i++)
	{
	    int n;
	    int m;
	    int x;
	    scanf("%d",&n);
	    scanf("%d",&m);
	    scanf("%d",&x);
	    int a[n];
	    int b[m];
	    int f=1;
	    for(int j=0;j<n;j++)
	    {
	        scanf("%d",&a[j]);
	    }
	    for(int j=0;j<m;j++)
	    {
	        scanf("%d",&b[j]);
	    }
	    for(int j=0;j<n;j++)
	    {
	        for(int k=0;k<m;k++)
	        {
	            if(a[j]+b[k]==x)
	            {
	                f=0;
	                printf("%d %d,",a[j],b[k]);
	               // /a[j]=0;
	               // b[k]=0;
	            }
	        }
	    }
	   // printf("\n%d\n",f); -5 89,24 60,29 55,30 54,34 50,43 41,45 39,58 26,81 3,86 -2,
	    if(f)
	    {
	        printf("-1");
	    }
	}
	return 0;
}